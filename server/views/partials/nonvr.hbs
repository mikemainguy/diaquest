<div class="nonvr">
    <button id="joinButton">Disconnected
        <div>Click to Connect</div>
    </button>
    <button id="leaveButton">Connected
        <div>Click to Leave</div></button>
    <button id="muteButton">Live
        <div>Click to Mute</div></button>
    <button id="unmuteButton">Muted
        <div>Click to Talk</div></button>
</div>
<script>
    document.querySelector('#joinButton').addEventListener('click', (evt) => {
        document.dispatchEvent(
                new CustomEvent('connectSignalwire',
                        {detail: 'OK'}));
        document.querySelector('#leaveButton').style.display='inline-block';
        document.querySelector('#joinButton').style.display='none';
        document.querySelector('#muteButton').style.display='inline-block';

    });
    document.querySelector('#leaveButton').addEventListener('click', (evt) => {
        document.dispatchEvent(
                new CustomEvent('disconnectSignalwire',
                        {detail: 'OK'}));
        document.querySelector('#leaveButton').style.display='none';
        document.querySelector('#joinButton').style.display='inline-block';
        document.querySelector('#muteButton').style.display='none';
        document.querySelector('#unmuteButton').style.display='none';

    });
    document.querySelector('#muteButton').addEventListener('click', (evt) => {
        document.dispatchEvent(
                new CustomEvent('mute',
                        {detail: 'OK'}));
        document.querySelector('#muteButton').style.display='none';
        document.querySelector('#unmuteButton').style.display='inline-block';

    });
    document.querySelector('#unmuteButton').addEventListener('click', (evt) => {
        document.dispatchEvent(
                new CustomEvent('unmute',
                        {detail: 'OK'}));
        document.querySelector('#muteButton').style.display='inline-block';
        document.querySelector('#unmuteButton').style.display='none';
    });
</script>